<%= provide(:title, sanitize(@post.title)) %>

<%= render 'categories/partials/twitter_cards' %>

<%= link_to "Edit Post", edit_category_group_post_path(@category, @group, @post) if admin_signed_in? %>
<%= link_to "Delete", category_group_post_path, method: :delete if admin_signed_in? %>

<div class="full-width-container">
  <div class="container">

    <div class="category-title">
      <%= @category.title %>
    </div>
    <div class="group-title">
      <%= @group.title %>
    </div>

    <div class="content group-container">

      <%= render 'categories/partials/previous_next' %>

      <%= render 'partials/ad_trash' %>

      <div class="front-container">
        <div class="one-space">
          <%= image_tag(@post.image, height: '100%', width: '100%' ) %>
        </div>
        <div class="bottom-left">
          <%= link_to @post.image_source, @post.image_source_link, class: "second-link" if @post.image_source.present? %>
        </div>
      </div>

      <div class="post-title padding-horizontal padding-vertical">
        <%= @post.title %>
      </div>

      <div class="best padding-horizontal">
        <%= sanitize(@post.content) %>
      </div>

      <div class="three-space padding-horizontal">
        <%= link_to @post.anchor, @post.link if @post.link.present? %>
      </div>

      <%= render 'partials/ad_trash' %>

      <%= render 'categories/partials/previous_next' %>

      <div class="padding-horizontal padding-vertical">
        <div class="one-space">
          Share this on:
        </div>
        <a onclick="javascript:window.open('https://www.facebook.com/sharer/sharer.php?u=<%= category_group_url(@category, @group) %>', '_blank', 'width=800, height=500, top=200, left=300');void(0)" class="facebook">Facebook</a>
        <a onclick="javascript:window.open('http://twitter.com/share?text=<%= @group.title %> - &amp;url=<%= category_group_url(@category, @group) %>', '_blank', 'width=800, height=500, top=200, left=300');void(0)" class="twitter">Twitter</a>
      </div>

      <%= render 'categories/partials/keep_up' %>

    </div>

  </div>
</div>